target-version = "py310"
line-length = 100

[lint]
select = ["E", "W", "F", "I"]
ignore = ["E501"]  # line-length lo maneja line-length, no E501

[lint.per-file-ignores]
"tests/*" = ["E402"]  # imports después de env setup en conftest, intencional
